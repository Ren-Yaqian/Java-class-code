package v1;

// 定义一个自己的日期类
// 最主要的目的，可以计算两个日期类之间相隔多少天 —— 计算差
public class MyDate {
    // 属性有些什么？  3 个：年、月、日
    // 年、月、日都可以使用 int 类型
    public int year;
    public int month;
    public int day;


    // 关键的方法 —— 计算两个日期之间的差/间隔
    public int 相差天数(MyDate from) {
        return -1;
    }
}
